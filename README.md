# 个人博客网站

一个使用 Supabase（数据库和后端服务）和 Netlify（前端部署）构建的现代化个人博客网站。

## 功能特性

- 📝 博客文章管理
- 🏷️ 文章分类系统
- 💬 评论功能
- 📊 阅读量统计
- 📱 响应式设计
- 🎨 现代化UI界面

## 技术栈

- **前端**: React + Vite + React Router
- **后端/数据库**: Supabase (PostgreSQL)
- **部署**: Netlify

## 项目结构

```
├── src/
│   ├── components/      # 组件
│   │   └── Layout.jsx   # 布局组件
│   ├── pages/          # 页面
│   │   ├── Home.jsx     # 首页
│   │   ├── BlogList.jsx # 博客列表页
│   │   └── BlogDetail.jsx # 博客详情页
│   ├── lib/
│   │   └── supabase.js  # Supabase客户端配置
│   ├── App.jsx          # 主应用组件
│   └── main.jsx         # 入口文件
├── supabase/
│   ├── schema.sql       # 数据库表结构
│   └── functions.sql    # 数据库函数
├── netlify.toml         # Netlify部署配置
└── package.json
```

## 数据库设计

### 1. categories 表（分类表）
- `id`: UUID主键
- `name`: 分类名称
- `description`: 分类描述
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 2. posts 表（文章表）
- `id`: UUID主键
- `title`: 文章标题
- `content`: 文章内容
- `excerpt`: 文章摘要
- `category_id`: 分类ID（外键）
- `author`: 作者
- `view_count`: 阅读量
- `published`: 是否发布
- `created_at`: 创建时间
- `updated_at`: 更新时间

### 3. comments 表（评论表）
- `id`: UUID主键
- `post_id`: 文章ID（外键）
- `author`: 评论者姓名
- `content`: 评论内容
- `email`: 评论者邮箱
- `approved`: 是否审核通过
- `created_at`: 创建时间
- `updated_at`: 更新时间

## 安装和运行

### 1. 安装依赖

```bash
npm install
```

### 2. 配置 Supabase

1. 在 [Supabase](https://supabase.com) 创建新项目
2. 在 Supabase SQL Editor 中执行 `supabase/schema.sql` 和 `supabase/functions.sql`
3. 获取项目的 URL 和 Anon Key
4. 创建 `.env` 文件：

```env
VITE_SUPABASE_URL=你的Supabase项目URL
VITE_SUPABASE_ANON_KEY=你的Supabase Anon Key
```

### 3. 运行开发服务器

```bash
npm run dev
```

### 4. 构建生产版本

```bash
npm run build
```

## 部署到 Netlify

### 方法一：通过 Netlify CLI

1. 安装 Netlify CLI：
```bash
npm install -g netlify-cli
```

2. 登录 Netlify：
```bash
netlify login
```

3. 初始化并部署：
```bash
netlify init
netlify deploy --prod
```

### 方法二：通过 GitHub

1. 将代码推送到 GitHub 仓库
2. 在 [Netlify](https://netlify.com) 登录并创建新站点
3. 连接 GitHub 仓库
4. 构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
5. 添加环境变量：
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
6. 部署

## 页面说明

### 1. 首页 (/)
- 显示欢迎信息和最新文章预览
- 展示最近6篇文章
- 提供查看所有文章的链接

### 2. 博客列表页 (/blog)
- 显示所有已发布的文章
- 支持按分类筛选
- 显示文章摘要和元信息

### 3. 博客详情页 (/blog/:id)
- 显示完整文章内容
- 显示文章评论
- 支持发表新评论（需审核）

## 注意事项

1. **安全性**: 在生产环境中，建议使用 Row Level Security (RLS) 策略保护数据
2. **评论审核**: 新评论默认需要审核，可在 Supabase 后台手动审核
3. **环境变量**: 确保在 Netlify 中正确配置环境变量
4. **CORS**: Supabase 已处理 CORS，无需额外配置

## 许可证

MIT

